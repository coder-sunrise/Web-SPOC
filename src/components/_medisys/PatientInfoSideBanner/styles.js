export default (theme) => ({
  entityName: { marginBottom: theme.spacing(1), fontWeight: 500 },
  schemeContainer: {
    textAlign: 'left',
    fontSize: '0.8rem',
    paddingTop: theme.spacing(1),
    overflow: 'auto',
  },
})
