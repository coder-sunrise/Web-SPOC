export default (theme) => ({
  cardContainer: {
    margin: theme.spacing(1),
  },
  summaryContainer: {
    marginTop: theme.spacing(2),
  },
  summaryContent: {
    paddingTop: theme.spacing(1),
  },
  divider: {
    width: '100%',
    background: 'black',
  },
})
